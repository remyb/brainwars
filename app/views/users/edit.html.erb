<h2>Edit user</h2>

<% form_for(@user) do |f| %>
	<%= render :partial => 'fields', :locals => { :f => f } %>
  	<div class="actions">
    	<%= f.submit "Update" %>
  	</div>
<% end %>

<div>
	<%= gravatar_for @user %>
	<a href="http://gravatar.com/emails" target="_blank">change</a>
</div>